<h1>My tasks</h1>

<ul>
  <% @tasks.each do |task| %>
    <li><%= link_to task.title,  task_path(task.id)%></li>
<!--     # link to first arg. what the user will click on
    # second argument the path, in this case the task path of the task with
    # this id -->
<!--     <%= link_to task.title,  task_path(task.id)%> -->
  <% end %>
</ul>
